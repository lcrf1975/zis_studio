name: CodeQL Analysis
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:
jobs:
  analyze:
    name: Analyze
    runs-on: ${{ matrix.language == 'swift' && 'macos-latest' || matrix.language == 'csharp' && 'windows-latest' || 'ubuntu-latest' }}
    permissions:
      security-events: write
      actions: read
      contents: read
    strategy:
      fail-fast: false
      matrix:
        language: [ 'python' ]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
      - name: Autobuild
        uses: github/codeql-action/autobuild@v3
        if: ${{ matrix.language != 'javascript' && matrix.language != 'python' && matrix.language != 'ruby' && matrix.language != 'csharp' && matrix.language != 'swift' }}
      - name: Setup MSBuild
        if: ${{ matrix.language == 'csharp' }}
        uses: microsoft/setup-msbuild@v2
      - name: Build C#
        if: ${{ matrix.language == 'csharp' }}
        run: msbuild /t:rebuild
      - name: Build Swift
        if: ${{ matrix.language == 'swift' }}
        run: |
          scheme=$(xcodebuild -list -json 2>/dev/null | python3 -c "import sys,json; d=json.load(sys.stdin); print((d.get('project') or d.get('workspace',{})).get('schemes',[''])[0])" 2>/dev/null)
          xcodebuild build -scheme "${scheme}" CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
